/*
 * R0:hello agi · 编程实战扩展题库（每阶段 2 道，共 106 道）
 * 制作者 / Creator:    Bilibili 黄豆666 (huangdouplone)
 * 版权所有 / Copyright: (c) 2026 黄豆666 / huangdouplone. All rights reserved.
 *
 * 说明：本文件为"叠加层"，通过 window.AGI_LAB_EXTRA 在启动时合并进各阶段的 lab 列表，
 * 不改动 cpp-extra-data.js 原有数据。字段与原有 lab 保持一致：
 *   { t, req:[], starter, hint, xp, mode?:"text", t_en, req_en?, hint_en? }
 */
window.AGI_LAB_EXTRA = {

/* ---------- 基础语法篇 ---------- */
"s1":[
 {t:"多行自我介绍卡片",t_en:"Multi-line Intro Card",
  req:["用若干条 cout 输出一个带分隔线的个人名片（昵称 / 座右铭 / 本周目标）","至少输出 5 行，并用 ----- 之类的分隔线区分头部与正文","每条输出语句单独占一行代码"],
  req_en:["Print a personal card (nickname / motto / weekly goal) using several cout statements","At least 5 lines, with a separator line such as ----- between header and body","Each output statement on its own line of code"],
  starter:`#include <iostream>\n\nint main() {\n    // 1. 输出分隔线\n    // 2. 输出昵称 / 座右铭 / 本周目标\n    // 3. 再输出一条分隔线\n\n    return 0;\n}`,
  hint:"重复调用 std::cout << \"...\" << std::endl; 即可；字符串里也能直接写空格和符号。",
  hint_en:"Just call std::cout << \"...\" << std::endl; repeatedly — spaces and symbols are fine inside the string.",
  xp:20},
 {t:"编译错误侦探",t_en:"Compile Error Detective",mode:"text",
  req:["下面这段代码有 3 处错误，逐条指出错在哪、为什么错、怎么改","给出修改后的完整可编译版本","说明编译器第一行报错通常指向什么"],
  req_en:["This snippet has 3 errors — point out each one, why it is wrong, and how to fix it","Give the full corrected compilable version","Explain what the compiler's first error line usually points to"],
  starter:`#include <iostream>\nInt Main() {\n    std::cout << "hi" << std::endl\n    return 0\n}`,
  hint:"大小写、分号、括号——新手 90% 的编译错误都在这三样里。",
  hint_en:"Case, semicolons, brackets — 90% of beginner compile errors live in these three.",
  xp:20}
],

"s2":[
 {t:"零钱换算器",t_en:"Coin Change Calculator",
  req:["读入一个代表金额的 double（单位：元）","换算成 1 元 / 5 角 / 1 角硬币的最少枚数并输出","注意浮点精度：建议先转成 int 分再计算"],
  req_en:["Read a double representing an amount in yuan","Convert to the minimum number of 1-yuan / 5-jiao / 1-jiao coins and print","Beware float precision — convert to integer cents first"],
  starter:`#include <iostream>\n\nint main() {\n    double money;\n    std::cin >> money;\n    int cents = static_cast<int>(money * 100 + 0.5);\n    // 用 cents 计算三种硬币数量\n\n    return 0;\n}`,
  hint:"先算 1 元枚数 = cents/100，剩下的 cents%=100 再算 5 角 = rest/50，余下就是 1 角。",
  hint_en:"First yuan = cents/100, then cents %= 100; jiao5 = rest/50; the remainder is jiao1.",
  xp:25},
 {t:"变量作用域观察实验",t_en:"Variable Scope Experiment",mode:"text",
  req:["读代码，写出三处 cout 分别输出什么；若某处会编译错误，请说明原因","把内层 int x 改成 x = 99（不重新声明），结果会怎么变？","用一句话总结「同名遮蔽」的规则"],
  req_en:["Read the code and write what each of the three cout statements prints; if one fails to compile, explain why","If you change the inner int x to x = 99 (no redeclaration), how does the result change?","Summarize the 'name shadowing' rule in one sentence"],
  starter:`int x = 1;\nstd::cout << x;      // A\n{\n    int x = 2;\n    std::cout << x;  // B\n}\nstd::cout << x;      // C`,
  hint:"内层 {} 里的 int x 是全新变量，只在该块内有效，出了块就消失。",
  hint_en:"The int x inside {} is a brand-new variable that only lives within that block.",
  xp:20}
],

"s3":[
 {t:"时间换算（秒 → 时:分:秒）",t_en:"Seconds to H:M:S",
  req:["读入一个整数总秒数","换算成 时:分:秒 并按下例格式输出（不足两位补 0）","例如输入 3661 输出 01:01:01"],
  req_en:["Read an integer total number of seconds","Convert to H:M:S and print in the sample format (zero-padded to 2 digits)","E.g. input 3661 outputs 01:01:01"],
  starter:`#include <iostream>\n#include <iomanip>\n\nint main() {\n    int total;\n    std::cin >> total;\n    // h = total/3600, m = ..., s = ...\n\n    return 0;\n}`,
  hint:"std::setw(2) << std::setfill('0') 可以补前导零。",
  hint_en:"std::setw(2) << std::setfill('0') pads with leading zeros.",
  xp:25},
 {t:"闰年判断器",t_en:"Leap Year Checker",
  req:["读入年份 year（int）","能被 400 整除，或能被 4 整除但不能被 100 整除 → 闰年","输出「是闰年」或「不是闰年」，并打印你的判断表达式"],
  req_en:["Read an integer year","Divisible by 400, or divisible by 4 but not by 100 → leap year","Print whether it is a leap year, and show your boolean expression"],
  starter:`#include <iostream>\n\nint main() {\n    int year;\n    std::cin >> year;\n    // bool leap = ...;\n\n    return 0;\n}`,
  hint:"写成 (year%400==0) || (year%4==0 && year%100!=0)，注意 && 优先级高于 ||，括号别省。",
  hint_en:"Write (year%400==0) || (year%4==0 && year%100!=0); keep the parentheses even though && binds tighter.",
  xp:25}
],

"s4":[
 {t:"成绩分布直方图",t_en:"Score Histogram",
  req:["读入 n 和 n 个成绩，统计 0-59 / 60-69 / 70-79 / 80-89 / 90-100 五档人数","用 * 画出横向直方图，每颗星代表 1 人","最后输出最高分与最低分"],
  req_en:["Read n and n scores; count how many fall in 0-59 / 60-69 / 70-79 / 80-89 / 90-100","Draw a horizontal histogram with *, one star per student","Finally print the max and min score"],
  starter:`#include <iostream>\n#include <vector>\n\nint main() {\n    int n; std::cin >> n;\n    int bucket[5] = {0,0,0,0,0};\n    // 读成绩并分档\n\n    return 0;\n}`,
  hint:"先算档位 idx，再 bucket[idx]++；画星用内层 for 循环。",
  hint_en:"Compute the bucket index, then bucket[idx]++; use an inner for loop to draw the stars.",
  xp:30},
 {t:"猜数字小游戏",t_en:"Guess the Number",
  req:["程序心里想一个 1~100 的随机数，玩家反复输入猜测","提示「大了」或「小了」，猜中后输出用了几次并结束","用 while 循环 + break 实现"],
  req_en:["The program picks a random number 1~100; the player keeps guessing","Reply 'too big' or 'too small'; when correct print the number of tries and exit","Use a while loop plus break"],
  starter:`#include <iostream>\n#include <cstdlib>\n#include <ctime>\n\nint main() {\n    std::srand(std::time(nullptr));\n    int target = std::rand() % 100 + 1;\n    int tries = 0;\n    while (true) {\n        // 读猜测、比较、提示、猜中 break\n    }\n    return 0;\n}`,
  hint:"循环里别忘了 tries++；猜中才 break，否则一直循环。",
  hint_en:"Don't forget tries++ inside the loop; break only when the guess matches.",
  xp:30}
],

"s5":[
 {t:"函数版四则运算器",t_en:"Function-based Calculator",
  req:["写 4 个函数 add / sub / mul / div，各接收两个 double 返回 double","除法函数在除数为 0 时返回 0 并打印警告","main 里读入 op 和两个数，用 switch 调用对应函数"],
  req_en:["Write add / sub / mul / div, each taking two doubles and returning a double","The division function returns 0 and prints a warning when the divisor is 0","In main, read an operator and two numbers, then dispatch with switch"],
  starter:`#include <iostream>\n\n// 在这里写四个函数\n\nint main() {\n    char op; double a, b;\n    std::cin >> a >> op >> b;\n    // switch 调用\n    return 0;\n}`,
  hint:"函数声明要写在 main 之前（或先声明原型）；switch 的 case 是字符要写 case '+':。",
  hint_en:"Define the functions before main (or forward-declare them); switch cases use case '+':.",
  xp:30},
 {t:"递归阶乘与斐波那契",t_en:"Recursive Factorial & Fibonacci",
  req:["用递归实现 factorial(n) 与 fib(n)","main 读入 n，输出 n! 与前 n 项斐波那契","在代码注释里写出 fib 的递归终止条件"],
  req_en:["Implement factorial(n) and fib(n) recursively","main reads n, prints n! and the first n Fibonacci numbers","Write fib's base case in a comment"],
  starter:`long long factorial(int n) {\n    // 终止条件 + 递归式\n}\n\nlong long fib(int n) {\n    // 终止条件 + 递归式\n}`,
  hint:"factorial(0)=1；fib(1)=fib(2)=1，fib(n)=fib(n-1)+fib(n-2)。",
  hint_en:"factorial(0)=1; fib(1)=fib(2)=1, fib(n)=fib(n-1)+fib(n-2).",
  xp:30}
],

"s6":[
 {t:"数组统计小工具",t_en:"Array Statistics Tool",
  req:["读入 n（≤100）和 n 个整数存入数组","输出平均值（保留 2 位小数）、最大值、最小值及其下标","不允许使用 vector，只能用原生数组"],
  req_en:["Read n (≤100) and n integers into an array","Print the average (2 decimals), max, min and their indices","Use a raw array, not vector"],
  starter:`#include <iostream>\n#include <iomanip>\n\nint main() {\n    int n; std::cin >> n;\n    int a[100];\n    // 读入并打擂台找最大最小\n\n    return 0;\n}`,
  hint:"打擂台法：先把 maxIdx=0，再逐个比较更新下标而不是值。",
  hint_en:"Tournament method: start with maxIdx=0 and update the index (not just the value).",
  xp:30},
 {t:"字符串回文判断",t_en:"Palindrome Checker",
  req:["读入一行英文字符串（可能含空格，用 getline）","忽略大小写与空格后判断是否为回文","输出判断结果与处理后的字符串"],
  req_en:["Read one line (may contain spaces, use getline)","Ignore case and spaces, then decide whether it is a palindrome","Print the verdict and the normalized string"],
  starter:`#include <iostream>\n#include <string>\n#include <algorithm>\n\nint main() {\n    std::string s;\n    std::getline(std::cin, s);\n    // 清洗 + 双指针判断\n\n    return 0;\n}`,
  hint:"std::tolower 转小写；用左右两个下标向中间靠拢比较。",
  hint_en:"Use std::tolower to normalize; compare with two indices moving toward the center.",
  xp:30}
],

"s7":[
 {t:"指针交换两数",t_en:"Swap Two Numbers via Pointers",
  req:["写 void swap2(int* a, int* b) 用指针交换两个整数","main 读入两个数，调用后打印交换结果","再写一个引用版本 swapRef(int&, int&) 并对比两者写法差异"],
  req_en:["Write void swap2(int* a, int* b) that swaps two integers through pointers","main reads two numbers, calls it, prints the result","Also write a reference version swapRef(int&, int&) and compare the two styles"],
  starter:`void swap2(int* a, int* b) {\n    // 通过 *a / *b 交换\n}\n\nvoid swapRef(int& a, int& b) {\n    // 直接交换\n}`,
  hint:"指针版要写 *a，调用时传 &x；引用版直接写 a，调用时传 x。",
  hint_en:"The pointer version uses *a and is called with &x; the reference version uses a and is called with x.",
  xp:30},
 {t:"动态数组成绩录入",t_en:"Dynamic Array Score Entry",
  req:["运行时读入人数 n，用 new 申请 int 数组","录入 n 个成绩后计算平均分","用 delete[] 释放，并把指针置为 nullptr"],
  req_en:["Read the number of students n at runtime and allocate an int array with new","Enter n scores, then compute the average","Release with delete[] and set the pointer to nullptr"],
  starter:`int n;\nstd::cin >> n;\nint* scores = new int[n];\n// 录入 + 求平均\n// delete[] scores; scores = nullptr;`,
  hint:"new 和 delete[] 必须成对；释放后置空可以避免野指针误用。",
  hint_en:"new and delete[] must come in pairs; nulling the pointer avoids dangling-pointer misuse.",
  xp:30}
],

"s8":[
 {t:"学生结构体排序",t_en:"Sort Students with struct",
  req:["定义 struct Student { string name; int score; }","读入 3 名学生的姓名与成绩存入数组","按成绩从高到低排序后输出排名表"],
  req_en:["Define struct Student { string name; int score; }","Read names and scores of 3 students into an array","Sort by score descending and print a ranking table"],
  starter:`#include <iostream>\n#include <string>\n#include <algorithm>\n\nstruct Student {\n    std::string name;\n    int score;\n};\n\nint main() {\n    // 读入 + 排序\n    return 0;\n}`,
  hint:"std::sort 第三个参数传 lambda：[](const Student& a, const Student& b){ return a.score > b.score; }。",
  hint_en:"Pass a lambda as std::sort's third argument: [](const Student& a, const Student& b){ return a.score > b.score; }.",
  xp:30},
 {t:"枚举星期转换器",t_en:"Enum Weekday Converter",
  req:["定义 enum class Weekday { Mon, Tue, ... Sun }","读入 1~7，用 switch 把它转成枚举并输出英文星期名","输入非法时输出错误提示"],
  req_en:["Define enum class Weekday { Mon, Tue, ... Sun }","Read 1~7, convert it to the enum with switch and print the English weekday name","Print an error message on invalid input"],
  starter:`enum class Weekday { Mon, Tue, Wed, Thu, Fri, Sat, Sun };\n\nint main() {\n    int d; std::cin >> d;\n    // switch 转换 + default 报错\n    return 0;\n}`,
  hint:"enum class 取值要写 Weekday::Mon；它不会隐式转 int，更安全。",
  hint_en:"enum class requires Weekday::Mon and does not implicitly convert to int — that's the safety benefit.",
  xp:25}
],

/* ---------- 数学基础篇（手算型，mode:"text"） ---------- */
"ma1":[
 {t:"手算点积与矩阵乘法",t_en:"Dot Product & Matrix Multiply by Hand",mode:"text",
  req:["计算 a=[1,2,3] 与 b=[4,5,6] 的点积，写出过程","计算 A(2×3) 与 B(3×2) 的乘积矩阵，写出每个元素的计算式","说明为什么神经网络里 y = Wx + b 本质上就是矩阵乘法"],
  req_en:["Compute the dot product of a=[1,2,3] and b=[4,5,6], showing your work","Compute the product of A(2x3) and B(3x2), writing out each entry","Explain why y = Wx + b in a neural net is essentially matrix multiplication"],
  starter:`a·b = ?\n\nA = [[1,2,3],[4,5,6]]\nB = [[7,8],[9,10],[11,12]]\nAB = ?`,
  hint:"点积是对应位相乘再求和；矩阵乘第 i 行第 j 列 = A 的第 i 行点乘 B 的第 j 列。",
  hint_en:"Dot product = elementwise multiply then sum; entry (i,j) = row i of A dot column j of B.",
  xp:25},
 {t:"NumPy 验证你的手算",t_en:"Verify Your Hand Math with NumPy",
  req:["用 NumPy 写脚本验证上一题的矩阵乘法结果","打印结果矩阵的 shape 与 dtype","用 @ 运算符和 np.dot 各算一次并对比"],
  req_en:["Write a NumPy script verifying the previous matrix product","Print the result's shape and dtype","Compute it once with the @ operator and once with np.dot, then compare"],
  starter:`import numpy as np\n\nA = np.array([[1,2,3],[4,5,6]])\nB = np.array([[7,8],[9,10],[11,12]])\n# 用 @ 和 np.dot 各算一次`,
  hint:"@ 是 Python 3.5+ 的矩阵乘运算符，等价于 np.matmul。",
  hint_en:"@ is Python 3.5+'s matrix-multiply operator, equivalent to np.matmul.",
  xp:25}
],

"ma2":[
 {t:"动手求秩与特征值",t_en:"Compute Rank & Eigenvalues by Hand",mode:"text",
  req:["求矩阵 [[1,2],[2,4]] 的秩，并说明它的两个列向量有什么关系","求 [[2,0],[0,3]] 的特征值与特征向量","解释「秩不满」在真实数据里通常意味着什么"],
  req_en:["Find the rank of [[1,2],[2,4]] and describe the relation between its two columns","Find the eigenvalues and eigenvectors of [[2,0],[0,3]]","Explain what 'rank deficient' usually means in real data"],
  starter:`A = [[1,2],[2,4]]\nrank(A) = ?\n\nB = [[2,0],[0,3]]\nλ = ?  v = ?`,
  hint:"第二列是第一列的 2 倍 → 线性相关 → 秩为 1；对角矩阵的特征值就是对角元。",
  hint_en:"Column 2 = 2 x column 1 → linearly dependent → rank 1; a diagonal matrix's eigenvalues are its diagonal entries.",
  xp:25},
 {t:"PCA 降维直觉实验",t_en:"PCA Intuition Experiment",
  req:["用 NumPy 生成一组二维相关数据（y ≈ 2x + 噪声）","手动用 np.cov 求协方差矩阵，np.linalg.eig 求特征向量","把数据投影到第一主成分上，画出降维前后的散点图"],
  req_en:["Generate correlated 2D data with NumPy (y ≈ 2x + noise)","Compute the covariance with np.cov and eigenvectors with np.linalg.eig","Project the data onto the first principal component and plot before/after"],
  starter:`import numpy as np\nimport matplotlib.pyplot as plt\n\nx = np.random.randn(200)\ny = 2*x + np.random.randn(200)*0.3\n# 协方差 -> 特征分解 -> 投影`,
  hint:"第一主成分就是协方差矩阵最大特征值对应的特征向量方向。",
  hint_en:"The first principal component is the eigenvector of the covariance matrix with the largest eigenvalue.",
  xp:30}
],

"ma3":[
 {t:"手算导数与梯度",t_en:"Derivatives & Gradient by Hand",mode:"text",
  req:["求 f(x)=x²+3x 在 x=2 处的导数","求 f(x,y)=x²+2y² 的梯度 ∇f","说明梯度方向与函数值上升/下降方向的关系"],
  req_en:["Compute the derivative of f(x)=x²+3x at x=2","Compute the gradient of f(x,y)=x²+2y²","Describe how the gradient direction relates to ascent/descent"],
  starter:`f(x) = x^2 + 3x\nf'(2) = ?\n\nf(x,y) = x^2 + 2y^2\n∇f = (?, ?)`,
  hint:"梯度指向函数上升最快的方向，所以梯度下降要沿 -∇f 走。",
  hint_en:"The gradient points toward steepest ascent, which is why gradient descent steps along -∇f.",
  xp:25},
 {t:"手写梯度下降",t_en:"Hand-written Gradient Descent",
  req:["用纯 Python 实现梯度下降最小化 f(x)=(x-3)²","记录每轮 x 与 f(x)，打印前 10 轮","尝试学习率 0.01 / 0.1 / 1.0，说明哪个会发散"],
  req_en:["Implement gradient descent in plain Python to minimize f(x)=(x-3)²","Log x and f(x) each step, print the first 10 iterations","Try learning rates 0.01 / 0.1 / 1.0 and say which diverges"],
  starter:`x = 0.0\nlr = 0.1\nfor i in range(50):\n    grad = 2*(x-3)\n    x -= lr*grad\n    # 打印`,
  hint:"学习率太大（接近或超过 1）会在最优点两侧来回震荡甚至发散。",
  hint_en:"Too large a learning rate oscillates around the optimum or diverges outright.",
  xp:30}
],

"ma4":[
 {t:"贝叶斯疾病筛查",t_en:"Bayesian Disease Screening",mode:"text",
  req:["患病率 1%，检测灵敏度 99%、特异度 95%，求检测阳性时真患病的概率","写出贝叶斯公式的完整代入过程","解释「基础概率谬误」为什么会让医生高估风险"],
  req_en:["Prevalence 1%, sensitivity 99%, specificity 95% — find P(disease | positive)","Write out the full Bayes formula substitution","Explain why the base-rate fallacy makes doctors overestimate risk"],
  starter:`P(D)=0.01, P(+|D)=0.99, P(+|~D)=0.05\nP(D|+) = ?`,
  hint:"P(D|+)=P(+|D)P(D) / [P(+|D)P(D)+P(+|¬D)P(¬D)] ≈ 0.17，远低于直觉。",
  hint_en:"P(D|+)=P(+|D)P(D) / [P(+|D)P(D)+P(+|¬D)P(¬D)] ≈ 0.17 — much lower than intuition.",
  xp:25},
 {t:"最大似然估计硬币",t_en:"MLE for a Coin",
  req:["抛硬币 10 次得到 7 次正面，写出似然函数并求 p 的 MLE","用 NumPy 在 p∈[0,1] 上扫描并画出似然曲线","说明样本量增大时估计值会怎样变化"],
  req_en:["10 flips give 7 heads — write the likelihood and find the MLE of p","Scan p over [0,1] with NumPy and plot the likelihood curve","Describe how the estimate changes as the sample grows"],
  starter:`import numpy as np\n\nps = np.linspace(0, 1, 101)\n# 计算 L(p) = p^7 * (1-p)^3 并找最大值`,
  hint:"二项分布的 MLE 就是正面次数 / 总次数 = 0.7。",
  hint_en:"For a binomial the MLE is simply heads / total = 0.7.",
  xp:30}
],

/* ---------- 面向对象篇 ---------- */
"s9":[
  {t:"静态成员与对象计数",t_en:"Static Members & Object Counter",
  req:["定义一个类，用 static int count 统计当前存活对象数：构造 +1、析构 -1","用 static 成员函数读计数（不依赖任何对象），在 main 里用类名直接调用","再用另一个 static 变量给每个对象分配自增 id，创建 3 个对象并打印各自的 id","说明静态成员属于类而不是对象，以及在类外为什么必须再定义一次"],
  req_en:["Define a class with a static int count tracking live objects, incremented in the constructor and decremented in the destructor","Add a static member function that reads the count without any object, and call it via the class name","Use another static variable to hand each object an auto-increment id; create three objects and print their ids","Explain that static members belong to the class, not an object, and why they must be defined once outside the class"],
  starter:"#include <iostream>\n\nclass Widget {\npublic:\n    static int count;\n    int id;\n    Widget() : id(++nextId) { ++count; }\n    ~Widget() { --count; }\n    static int alive() { return count; }\nprivate:\n    static int nextId;\n};\n\nint Widget::count = 0;\nint Widget::nextId = 0;\n\nint main() {\n    Widget a, b, c;\n    std::cout << Widget::alive() << std::endl;\n    return 0;\n}",
  hint:"静态成员变量在类内只是「声明」，必须在类外再写一次定义（int Widget::count = 0;），否则链接阶段报未定义符号。",
  hint_en:"A static data member inside the class is only a declaration — define it once outside (int Widget::count = 0;), or the linker reports an undefined symbol.",
  xp:35},
 {t:"对象生命周期观察",t_en:"Object Lifetime Observation",
  req:["给类加构造函数与析构函数，各打印一行标记","在 main 里创建对象、放进块作用域、用 new 创建","运行后按输出顺序说明每对构造/析构对应哪个对象"],
  req_en:["Add a constructor and destructor that each print a marker line","In main, create an object, one inside a block, and one with new","Explain which object each constructor/destructor pair belongs to, in output order"],
  starter:`class Tracer {\npublic:\n    Tracer()  { std::cout << "ctor\\n"; }\n    ~Tracer() { std::cout << "dtor\\n"; }\n};`,
  hint:"栈对象出了作用域自动析构；new 出来的必须手动 delete 才会析构。",
  hint_en:"Stack objects are destroyed at scope exit; objects from new need an explicit delete.",
  xp:25}
],

"s10":[
  {t:"构造析构顺序与虚析构",t_en:"Ctor-Dtor Order & Virtual Dtor",
  req:["写基类 Base 与派生类 Derived，各自在构造/析构里打印标记","创建再销毁一个 Derived 对象，按输出说明构造顺序（基类先、派生后）与析构顺序（派生先、基类后）","用 Base *p = new Derived; delete p; 在基类析构「非虚」与「虚」两种情况下各跑一次，观察 Derived 析构是否被调用","写注释说明为什么多态基类的析构必须是 virtual"],
  req_en:["Write Base and Derived classes each printing a tag in their constructor and destructor","Create then destroy a Derived object and use the output to show ctor order (base first) and dtor order (derived first)","Run Base *p = new Derived; delete p; with a non-virtual and then a virtual base destructor, noting whether Derived's dtor runs","Comment on why a polymorphic base class must have a virtual destructor"],
  starter:"#include <iostream>\n\nclass Base {\npublic:\n    Base() { std::cout << \"Base ctor\\n\"; }\n    ~Base() { std::cout << \"Base dtor\\n\"; }  // 改成 virtual 再跑一次\n};\n\nclass Derived : public Base {\npublic:\n    Derived() { std::cout << \"Derived ctor\\n\"; }\n    ~Derived() { std::cout << \"Derived dtor\\n\"; }\n};\n\nint main() {\n    Base *p = new Derived;\n    delete p;\n    return 0;\n}",
  hint:"析构非虚时用基类指针 delete 派生对象是未定义行为，通常只跑基类析构、派生部分泄漏；加 virtual ~Base() 后才会沿链正确析构。",
  hint_en:"A non-virtual destructor plus delete through a base pointer is undefined behaviour — usually only Base::~Base runs and the derived part leaks. virtual ~Base() fixes the chain.",
  xp:40},
 {t:"运算符重载：复数加法",t_en:"Operator Overloading: Complex Numbers",
  req:["定义 class Complex，重载 + 与 <<","使 Complex a(1,2), b(3,4); std::cout << a + b; 输出 4+6i","说明成员函数重载与友元重载的取舍"],
  req_en:["Define class Complex overloading + and <<","So that Complex a(1,2), b(3,4); std::cout << a + b; prints 4+6i","Discuss member vs friend overload and when to choose each"],
  starter:`class Complex {\n    double re_, im_;\npublic:\n    Complex(double r=0, double i=0) : re_(r), im_(i) {}\n    // 重载 + 与 <<\n};`,
  hint:"<< 必须写成友元（或自由函数），因为左操作数是流对象而不是你的类。",
  hint_en:"<< must be a friend (or free function) because the left operand is the stream, not your class.",
  xp:35}
],

"s11":[
 {t:"泛型最大值与交换",t_en:"Generic max & swap",
  req:["写函数模板 template<typename T> T myMax(T a, T b)","写 template<typename T> void mySwap(T& a, T& b)","用 int、double、std::string 三种类型各测一次并打印"],
  req_en:["Write template<typename T> T myMax(T a, T b)","Write template<typename T> void mySwap(T& a, T& b)","Test both with int, double and std::string, printing the results"],
  starter:`template<typename T>\nT myMax(T a, T b) {\n    return a > b ? a : b;\n}`,
  hint:"模板定义通常要放在头文件里（或在同一个编译单元内），否则链接期找不到实例化。",
  hint_en:"Templates usually live in headers (or the same translation unit), otherwise instantiation fails at link time.",
  xp:30},
 {t:"泛型动态数组 MiniVector",t_en:"Generic MiniVector",
  req:["写类模板 template<typename T> class MiniVector，内部用 new T[] 存储","实现 push_back / operator[] / size / 析构函数","测试存入 int 与 std::string 各一次"],
  req_en:["Write template<typename T> class MiniVector backed by new T[]","Implement push_back / operator[] / size / destructor","Test it once with int and once with std::string"],
  starter:`template<typename T>\nclass MiniVector {\n    T* data_;\n    int size_;\n    int cap_;\npublic:\n    // ctor / dtor / push_back / operator[] / size\n};`,
  hint:"容量不够时要 new 一块更大的空间、拷贝旧数据、delete[] 旧空间。",
  hint_en:"When capacity runs out, allocate a bigger block, copy the old data, then delete[] the old block.",
  xp:40}
],

"sdbg":[
 {t:"越界 Bug 定位与修复",t_en:"Find & Fix the Out-of-Bounds Bug",
  req:["下面代码在运行时崩溃或结果异常，指出越界发生在哪一行","说明为什么越界读写有时不立刻崩溃","给出修复后的完整代码"],
  req_en:["The code below crashes or misbehaves at runtime — say which line goes out of bounds","Explain why out-of-bounds access doesn't always crash immediately","Give the corrected full code"],
  starter:`#include <iostream>\nint main() {\n    int a[5] = {1,2,3,4,5};\n    for (int i = 0; i <= 5; i++) {\n        std::cout << a[i] << " ";\n    }\n    return 0;\n}`,
  hint:"循环条件写成 i<=5 会访问 a[5] —— 合法下标是 0~4。越界是未定义行为，可能「看起来正常」。",
  hint_en:"i<=5 accesses a[5] — valid indices are 0~4. Out-of-bounds is undefined behavior and may 'look fine'.",
  xp:30},
 {t:"调试器实操：断点与观察",t_en:"Debugger Practice: Breakpoints & Watch",mode:"text",
  req:["用 3~5 句话描述你在 VS Code / GDB 里给下面循环打断点并观察 sum 的完整步骤","说明「单步进入」与「单步跳过」的区别","记录一次你真实遇到并通过调试解决的 bug（没有就写一个模拟场景）"],
  req_en:["In 3–5 sentences, describe the exact steps to breakpoint the loop below and watch sum in VS Code / GDB","Explain 'step into' vs 'step over'","Log one bug you actually solved by debugging (or invent a plausible scenario)"],
  starter:`int sum = 0;\nfor (int i = 1; i <= 10; i++) {\n    sum += i;\n}\nstd::cout << sum;`,
  hint:"先怀疑数据、再怀疑逻辑；断点打在「值第一次变错的那一步之前」。",
  hint_en:"Suspect data first, logic second; put the breakpoint just before the step where the value first goes wrong.",
  xp:25}
],

/* ---------- STL 与工程篇 ---------- */
"s12":[
 {t:"vector 成绩册管理",t_en:"Grade Book with vector",
  req:["用 vector<int> 存成绩，支持循环读入直到输入 -1","实现求平均、删除指定下标、按升序排序三个操作","每步操作后打印当前 vector 内容"],
  req_en:["Use vector<int> for scores; read in a loop until -1 is entered","Implement average, erase-by-index and ascending sort","Print the vector contents after each operation"],
  starter:`#include <iostream>\n#include <vector>\n#include <algorithm>\n\nint main() {\n    std::vector<int> v;\n    // 读入 - 排序 - 删除 - 求平均\n    return 0;\n}`,
  hint:"erase 要用迭代器：v.erase(v.begin()+idx)；别在范围 for 里 erase。",
  hint_en:"erase takes an iterator: v.erase(v.begin()+idx); never erase inside a range-for.",
  xp:30},
 {t:"deque 与 list 选型对比",t_en:"deque vs list: Choosing a Container",mode:"text",
  req:["分别说明 vector / deque / list 在「头部插入、随机访问、中间插入」三件事上的复杂度","给出你会在什么场景选 deque 而不是 vector","用 30 行以内代码实测三者在头部插入 10 万次的耗时差异"],
  req_en:["Give the complexity of vector / deque / list for front-insert, random access and middle-insert","Say in which scenario you'd pick deque over vector","In under 30 lines, benchmark all three doing 100k front insertions"],
  starter:`// 用 std::chrono 计时\n// vector::insert(begin()) vs deque::push_front vs list::push_front`,
  hint:"vector 头部插入是 O(n) 因为要整体搬移；deque 是分块存储所以两端都快。",
  hint_en:"vector front-insert is O(n) because everything shifts; deque stores in blocks so both ends are fast.",
  xp:30}
],

"s13":[
 {t:"词频统计器",t_en:"Word Frequency Counter",
  req:["读入若干单词（以 EOF 结束），用 map<string,int> 统计词频","按词频降序输出前 5 名（map 不能直接排序，需搬到 vector 再排）","输出不同单词的总数"],
  req_en:["Read words until EOF and count them with map<string,int>","Print the top 5 by frequency (a map can't be sorted directly — move to a vector first)","Print the number of distinct words"],
  starter:`#include <iostream>\n#include <map>\n#include <vector>\n#include <algorithm>\n#include <string>\n\nint main() {\n    std::map<std::string, int> cnt;\n    // 统计 + 搬到 vector + 排序\n    return 0;\n}`,
  hint:"排序时用 lambda 比较 pair 的 second；相同词频可按字典序兜底。",
  hint_en:"Sort with a lambda comparing pair::second; break ties by the word for determinism.",
  xp:35},
 {t:"set 交集与去重",t_en:"set Intersection & Deduplication",
  req:["读入两组整数，用 set 去重并排序","用 std::set_intersection 求交集并输出","对比 unordered_set 在纯去重场景下的差异并说明原因"],
  req_en:["Read two sets of integers; deduplicate and sort with set","Compute and print the intersection with std::set_intersection","Contrast with unordered_set for pure dedup and explain the difference"],
  starter:`#include <set>\n#include <algorithm>\n#include <iterator>\n\nstd::set<int> a, b, inter;\n// set_intersection(a.begin(), a.end(), b.begin(), b.end(), inserter(inter, inter.begin()));`,
  hint:"set_intersection 要求两边都已排序；结果要用 std::inserter 写回。",
  hint_en:"set_intersection requires both ranges sorted; write results back via std::inserter.",
  xp:30}
],

"s14":[
 {t:"迭代器 + 算法重构循环",t_en:"Refactor Loops with Iterators & Algorithms",
  req:["把一段手写的「查找最大值下标」循环改写成 std::max_element","用 std::count_if + lambda 统计及格人数","说明为什么算法版比手写循环更不容易出错"],
  req_en:["Rewrite a hand-rolled 'find index of max' loop using std::max_element","Count passing grades with std::count_if plus a lambda","Explain why the algorithm version is less error-prone than a manual loop"],
  starter:`std::vector<int> v = {55, 78, 91, 62, 88};\nauto it = std::max_element(v.begin(), v.end());\nint idx = std::distance(v.begin(), it);`,
  hint:"算法表达「做什么」而非「怎么做」，边界条件由库保证。",
  hint_en:"Algorithms express what, not how — the library guarantees the boundary conditions.",
  xp:30},
 {t:"lambda 捕获方式实验",t_en:"Lambda Capture Experiment",mode:"text",
  req:["分别用 [=]、[&]、[x]、[&x] 捕获同一个变量，预测并验证输出结果差异","说明默认按值捕获时修改外部变量会发生什么","给出一条你在项目里选择捕获方式的经验规则"],
  req_en:["Capture the same variable with [=], [&], [x] and [&x]; predict and verify the different outputs","Say what happens when you modify an outer variable under default by-value capture","Give one rule of thumb you'd use to choose a capture mode in real projects"],
  starter:`int x = 1;\nauto f1 = [=]()  { return x; };\nauto f2 = [&]()  { return x; };\nx = 99;\n// f1() 与 f2() 分别是多少？`,
  hint:"[=] 在 lambda 创建时就拷贝了一份快照，之后再改 x 也不影响它。",
  hint_en:"[=] snapshots the value at creation time, so later changes to x don't affect it.",
  xp:25}
],

"s15":[
 {t:"通讯录文件持久化",t_en:"Contact Book File Persistence",
  req:["用 ofstream 把若干条「姓名,电话」写入 contacts.txt","重新用 ifstream 读出并逐行解析打印","打开失败时给出明确错误提示而不是静默"],
  req_en:["Write several name,phone records to contacts.txt with ofstream","Read them back with ifstream, parse line by line and print","Fail loudly with a clear message if opening fails"],
  starter:`#include <fstream>\n#include <string>\n\nstd::ofstream out("contacts.txt");\nif (!out) { std::cerr << "打开失败\\n"; return 1; }`,
  hint:"ifstream/ofstream 一定要检查 is_open() 或直接用 if(!out)，否则写失败你永远不知道。",
  hint_en:"Always check is_open() or use if(!out) — otherwise a failed write is silent.",
  xp:35},
 {t:"CSV 成绩表读写",t_en:"CSV Grade Table I/O",
  req:["读入 grades.csv（格式：姓名,数学,英语），计算每人总分","把「姓名,总分,等级」写回 result.csv","处理文件不存在 / 字段缺失两种异常输入"],
  req_en:["Read grades.csv (name,math,english) and compute each total","Write name,total,grade back to result.csv","Handle both a missing file and rows with missing fields"],
  starter:`#include <fstream>\n#include <sstream>\n\n// getline 读一行 -> stringstream 按逗号切分`,
  hint:"std::getline(ss, cell, ',') 可以指定分隔符，配合 stringstream 做简单 CSV 解析。",
  hint_en:"std::getline(ss, cell, ',') takes a delimiter — pair it with stringstream for simple CSV parsing.",
  xp:35}
],

"s16":[
 {t:"安全除法与异常",t_en:"Safe Division with Exceptions",
  req:["写 double safeDiv(double a, double b)，b 为 0 时 throw std::invalid_argument","main 用 try/catch 捕获并打印 e.what()","对比「返回错误码」与「抛异常」两种风格的取舍"],
  req_en:["Write double safeDiv(double a, double b) that throws std::invalid_argument when b is 0","Catch it in main and print e.what()","Compare the trade-offs of error codes vs exceptions"],
  starter:`#include <stdexcept>\n\ndouble safeDiv(double a, double b) {\n    if (b == 0) throw std::invalid_argument("除数为 0");\n    return a / b;\n}`,
  hint:"catch 用 const 引用：catch (const std::exception& e)，避免对象切片。",
  hint_en:"Catch by const reference — catch (const std::exception& e) — to avoid object slicing.",
  xp:30},
 {t:"RAII 文件句柄封装",t_en:"RAII File Handle Wrapper",
  req:["写一个类 FileGuard，构造时打开文件、析构时自动关闭","在可能抛异常的代码路径中使用它，验证异常发生时文件仍被关闭","说明 RAII 解决了什么问题"],
  req_en:["Write a FileGuard class that opens the file in its ctor and closes it in its dtor","Use it on a path that can throw, and verify the file still gets closed","Explain what problem RAII solves"],
  starter:`class FileGuard {\n    std::FILE* f_;\npublic:\n    explicit FileGuard(const char* path);\n    ~FileGuard();\n};`,
  hint:"资源获取即初始化：把资源生命周期绑到栈对象的生命周期上，异常也能正确释放。",
  hint_en:"Resource Acquisition Is Initialization ties a resource's lifetime to a stack object, so exceptions still release it.",
  xp:35}
],

"s17":[
 {t:"智能指针改造裸指针",t_en:"Replace Raw Pointers with Smart Pointers",
  req:["把一段用 new/delete 的代码改写成 unique_ptr","演示用 make_shared 创建并在两处共享同一个对象","说明什么时候仍需要 weak_ptr"],
  req_en:["Rewrite a new/delete snippet using unique_ptr","Create with make_shared and share the same object from two places","Say when you still need weak_ptr"],
  starter:`#include <memory>\n\nauto p = std::make_unique<int>(42);\nauto s = std::make_shared<int>(7);`,
  hint:"优先 make_unique / make_shared，它们既少写类型名又异常安全。",
  hint_en:"Prefer make_unique / make_shared — less typing and exception-safe.",
  xp:35},
 {t:"auto 与范围 for 重构",t_en:"Refactor with auto & Range-for",
  req:["把三段显式类型的循环改写成范围 for + auto","说明 auto&&、const auto&、auto 三者分别在什么场合用","找出一处「auto 让类型变模糊」的反例并说明"],
  req_en:["Rewrite three explicitly typed loops using range-for and auto","Explain when to use auto&&, const auto& and plain auto","Give one counter-example where auto obscures the type"],
  starter:`std::map<std::string, std::vector<int>> data;\nfor (const auto& kv : data) { /* ... */ }`,
  hint:"只读遍历用 const auto&，需要拷贝用 auto，转发场景才用 auto&&。",
  hint_en:"Use const auto& for read-only traversal, auto when you need a copy, auto&& only for forwarding.",
  xp:30}
],

"s18":[
 {t:"命令行计算器项目",t_en:"CLI Calculator Project",
  req:["支持交互式输入表达式（如 3 + 5 * 2），输出计算结果","支持 exit 退出，输入非法时提示并保持不崩溃","把「读取—解析—计算」拆成至少 3 个函数"],
  req_en:["Support interactive expressions like 3 + 5 * 2 and print results","Support exit; on invalid input print a hint without crashing","Split read / parse / evaluate into at least three functions"],
  starter:`#include <iostream>\n#include <string>\n\nint main() {\n    std::string line;\n    while (std::getline(std::cin, line)) {\n        // 解析并计算\n    }\n    return 0;\n}`,
  hint:"先用 stringstream 按空格切词，再按运算符优先级处理乘除。",
  hint_en:"Split tokens with stringstream first, then handle * and / before + and -.",
  xp:45},
 {t:"多文件项目拆分",t_en:"Split into a Multi-file Project",mode:"text",
  req:["把一个单文件程序拆成 main.cpp / calc.h / calc.cpp 三份，写出每份的内容提纲","给出一条能一次编译通过的 g++ 命令","说明为什么声明放 .h、定义放 .cpp"],
  req_en:["Split a single-file program into main.cpp / calc.h / calc.cpp; outline each file","Give one g++ command that compiles them in one shot","Explain why declarations go in .h and definitions in .cpp"],
  starter:`// g++ main.cpp calc.cpp -o app\n// 头文件用 #pragma once 或 include guard 防重复包含`,
  hint:"头文件只放声明，谁 include 谁就拿到接口；定义放 .cpp 才能保证只编译一次、链接一次。",
  hint_en:"Headers hold declarations so includers get the interface; definitions in .cpp guarantee compile-once, link-once.",
  xp:40}
],

/* ---------- 算法与数据结构篇 ---------- */
"sbw":[
 {t:"位运算技巧集",t_en:"Bit Manipulation Toolkit",
  req:["写函数判断一个整数是否为 2 的幂","写函数统计二进制中 1 的个数（用 n & (n-1) 技巧）","写函数在不使用临时变量的情况下交换两个数（异或法）并说明风险"],
  req_en:["Write a function testing whether an integer is a power of two","Count set bits using the n & (n-1) trick","Swap two numbers without a temporary (xor) and explain the risk"],
  starter:`bool isPow2(int n) { return n > 0 && (n & (n - 1)) == 0; }\n\nint popcount(int n) { int c = 0; while (n) { n &= n - 1; ++c; } return c; }`,
  hint:"n & (n-1) 会清掉最低位的 1；x ^= y ^= x ^= y 虽然炫但可读性差，实际项目别用。",
  hint_en:"n & (n-1) clears the lowest set bit; the xor swap is clever but unreadable — don't use it in real code.",
  xp:30},
 {t:"位掩码权限系统",t_en:"Bitmask Permission System",
  req:["用枚举定义 READ=1, WRITE=2, EXEC=4 等权限位","实现授予、撤销、判断是否拥有某权限三个函数","用二进制形式打印当前权限集合"],
  req_en:["Define READ=1, WRITE=2, EXEC=4 as permission bits","Implement grant, revoke and has-permission","Print the current permission set in binary"],
  starter:`enum Perm { READ = 1 << 0, WRITE = 1 << 1, EXEC = 1 << 2 };\nint mask = 0;\nmask |= READ;      // 授予\nmask &= ~WRITE;    // 撤销\n// 判断：(mask & READ) != 0`,
  hint:"授予用 |=，撤销用 &= ~，判断用 & —— 这三板斧覆盖绝大多数位掩码场景。",
  hint_en:"Grant with |=, revoke with &= ~, test with & — these three cover most bitmask use cases.",
  xp:30}
],

"srec":[
  {t:"记忆化递归优化",t_en:"Memoized Recursion",
  req:["写朴素递归 fib(n)，用一个全局计数器统计函数被调用了多少次，打印 fib(30) 的调用次数","加一个 memo 数组（初值 -1）做记忆化，再统计 fib(30) 的调用次数，对比两次的数量级差异","调用 fib(40) 验证记忆化版本仍能瞬间返回，并打印结果","说明记忆化为什么能把指数复杂度降到线性（重叠子问题）"],
  req_en:["Write a naive recursive fib(n) with a global counter for call count, and print the count for fib(30)","Add a memo array initialised to -1 for memoization, recount the calls for fib(30) and compare orders of magnitude","Call fib(40) to confirm the memoized version still returns instantly, and print the result","Explain why memoization collapses exponential time to linear (overlapping subproblems)"],
  starter:"#include <cstdio>\n\nlong long calls = 0;\n\nlong long fib(int n) {\n    ++calls;\n    if (n < 2) return n;\n    return fib(n - 1) + fib(n - 2);\n}\n\nint main() {\n    calls = 0;\n    printf(\"fib(30)=%lld  调用=%lld\\n\", fib(30), calls);\n    // 再用 memo 数组重写一版并对比调用次数\n    return 0;\n}",
  hint:"朴素 fib 的调用次数本身是指数级的（约 1.6^n）；加 memo 后在入口检查「已算过就直接返回」，每个 n 只算一次，降到 O(n)。",
  hint_en:"Naive fib makes an exponential number of calls (~1.6^n). With memo, checking already-computed at the entrance makes each n cost one call: O(n).",
  xp:35},
 {t:"归并排序手写实现",t_en:"Hand-written Merge Sort",
  req:["实现归并排序 mergeSort(arr, l, r)，含 merge 合并函数","用随机数组对比它与 std::sort 的结果是否一致","说明它的时间复杂度与空间复杂度，以及为什么是稳定排序"],
  req_en:["Implement mergeSort(arr, l, r) with a merge function","Compare its output against std::sort on a random array","Give its time and space complexity and explain why it is stable"],
  starter:`void merge(int a[], int l, int m, int r);\nvoid mergeSort(int a[], int l, int r) {\n    if (l >= r) return;\n    int m = l + (r - l) / 2;\n    // 分治 + 合并\n}`,
  hint:"合并时左右相等取左边，这就是稳定性的来源；额外空间是 O(n)。",
  hint_en:"Taking the left element on ties is exactly what makes it stable; extra space is O(n).",
  xp:40}
],

"sdp":[
 {t:"爬楼梯：从递归到 DP",t_en:"Climbing Stairs: Recursion to DP",
  req:["先写递归版 f(n)=f(n-1)+f(n-2)，观察 n=40 时的耗时","改成带备忘录的记忆化版本","再改成 O(1) 空间的滚动数组版本并对比"],
  req_en:["Write the recursive f(n)=f(n-1)+f(n-2) and observe how slow n=40 is","Convert it to memoization","Then to an O(1) rolling-array version, and compare"],
  starter:`long long climb(int n) {\n    if (n <= 2) return n;\n    long long a = 1, b = 2;\n    for (int i = 3; i <= n; ++i) { long long c = a + b; a = b; b = c; }\n    return b;\n}`,
  hint:"递归版慢是因为大量重复子问题；DP 的本质就是把算过的结果存起来。",
  hint_en:"The recursive version is slow because it recomputes overlapping subproblems — DP is just caching those results.",
  xp:35},
 {t:"0-1 背包入门",t_en:"Intro to 0-1 Knapsack",
  req:["给定 n 个物品的重量与价值，在容量 W 内求最大价值","先写二维 dp[i][w]，再优化成一维倒序遍历","说明为什么一维必须倒序遍历"],
  req_en:["Given weights and values for n items, maximize value within capacity W","Write the 2D dp[i][w] first, then optimize to 1D with reverse traversal","Explain why the 1D loop must go backwards"],
  starter:`// dp[w] 表示容量 w 能装的最大价值\nfor (int i = 0; i < n; ++i)\n    for (int w = W; w >= wt[i]; --w)\n        dp[w] = std::max(dp[w], dp[w - wt[i]] + val[i]);`,
  hint:"正序遍历会让同一个物品被重复放入，倒序才能保证每件只用一次。",
  hint_en:"Forward traversal lets the same item be reused; reverse order guarantees each item is used at most once.",
  xp:40}
],

"dsl":[
 {t:"手写单链表反转",t_en:"Reverse a Singly Linked List",
  req:["定义 struct ListNode { int val; ListNode* next; }","用迭代法（三指针）实现反转并返回新头结点","再写递归版，并说明两者的空间复杂度差异"],
  req_en:["Define struct ListNode { int val; ListNode* next; }","Reverse it iteratively with three pointers and return the new head","Also write a recursive version and compare space complexity"],
  starter:`ListNode* reverse(ListNode* head) {\n    ListNode *prev = nullptr, *cur = head;\n    while (cur) {\n        ListNode* nxt = cur->next;\n        cur->next = prev;\n        prev = cur; cur = nxt;\n    }\n    return prev;\n}`,
  hint:"先把 next 存下来再改指针，否则链表就断了。",
  hint_en:"Save next before rewiring, otherwise you lose the rest of the list.",
  xp:35},
 {t:"复杂度分析练习",t_en:"Complexity Analysis Drill",mode:"text",
  req:["给出下面三段代码的时间复杂度并说明理由","指出哪一段是 O(n log n)、哪一段是 O(n²)","说明「最好 / 最坏 / 平均」三种复杂度分别在什么场景有意义"],
  req_en:["Give the time complexity of the three snippets below, with reasoning","Say which is O(n log n) and which is O(n²)","Explain when best / worst / average complexity each matter"],
  starter:`// A: for(i=0;i<n;i++) for(j=i;j<n;j++)\n// B: while(n>1) n/=2\n// C: for(i=0;i<n;i++) for(j=0;j<32;j++)`,
  hint:"A 的内层从 i 开始，总次数约 n²/2 → O(n²)；B 每次减半 → O(log n)；C 的内层是常数 → O(n)。",
  hint_en:"A's inner loop starts at i, about n²/2 total → O(n²); B halves each time → O(log n); C's inner loop is constant → O(n).",
  xp:25}
],

"dsk":[
 {t:"有效括号匹配",t_en:"Valid Parentheses",
  req:["用栈判断只包含 ()[]{} 的字符串是否合法闭合","遇到右括号时栈顶必须匹配，否则返回 false","最后栈必须为空才算通过"],
  req_en:["Use a stack to check whether a string of ()[]{} closes correctly","On a closing bracket, the top of the stack must match, else return false","The stack must be empty at the end"],
  starter:`#include <stack>\n#include <string>\n\nbool isValid(const std::string& s) {\n    std::stack<char> st;\n    // 遇左括号入栈，遇右括号比对\n    return st.empty();\n}`,
  hint:"可以用 map 存配对关系，遇到右括号先检查栈非空再比对。",
  hint_en:"Store the pairs in a map; on a closing bracket check the stack is non-empty before comparing.",
  xp:35},
 {t:"哈希冲突实验",t_en:"Hash Collision Experiment",mode:"text",
  req:["用 std::hash 对 1000 个随机整数取哈希，统计模 100 后的桶分布","说明分布不均会对性能造成什么影响","给出两种缓解冲突的方法并比较"],
  req_en:["Hash 1000 random ints with std::hash and tally the bucket distribution mod 100","Explain what an uneven distribution does to performance","Give two collision-mitigation approaches and compare them"],
  starter:`std::map<size_t, int> buckets;\nfor (int x : data) buckets[std::hash<int>{}(x) % 100]++;`,
  hint:"链地址法实现简单、对负载因子宽容；开放寻址缓存友好但对装载率敏感。",
  hint_en:"Separate chaining is simple and tolerant of high load; open addressing is cache-friendly but load-sensitive.",
  xp:30}
],

"dst":[
 {t:"二叉搜索树插入与中序",t_en:"BST Insert & Inorder",
  req:["实现 BST 的插入与中序遍历","验证「中序遍历结果是升序」这一性质","实现查找某个值并返回查找深度"],
  req_en:["Implement BST insertion and inorder traversal","Verify that inorder traversal yields sorted order","Implement search returning the depth at which the value was found"],
  starter:`struct Node { int val; Node *left = nullptr, *right = nullptr; };\n\nvoid insert(Node*& root, int v) {\n    if (!root) { root = new Node{v}; return; }\n    if (v < root->val) insert(root->left, v);\n    else insert(root->right, v);\n}`,
  hint:"BST 的左子树都小于根、右子树都大于根，所以中序遍历自然是升序。",
  hint_en:"Everything left of a BST node is smaller and everything right is larger, so inorder comes out sorted.",
  xp:40},
 {t:"最小堆手写实现",t_en:"Hand-written Min-Heap",
  req:["用数组实现最小堆的 push / pop / siftUp / siftDown","实现一个 topK 函数：从大量数据中取最小的 k 个","说明为什么 topK 用堆比排序更快"],
  req_en:["Implement push / pop / siftUp / siftDown over an array","Implement topK: the k smallest of a large stream","Explain why a heap beats sorting for topK"],
  starter:`void siftUp(std::vector<int>& h, int i);\nvoid siftDown(std::vector<int>& h, int i);\n// 父 (i-1)/2，子 2i+1 / 2i+2`,
  hint:"维护一个大小固定为 k 的最大堆，遍历时只在更小才替换堆顶 —— 复杂度 O(n log k)。",
  hint_en:"Keep a max-heap of size k and replace the top only when something smaller arrives — O(n log k).",
  xp:40}
],

"dsg":[
  {t:"图的连通分量统计",t_en:"Connected Components",
  req:["读入 n、m 与 m 条边，用邻接表 vector<vector<int>> 表示一个无向图（顶点编号 1..n）","用 DFS 或 BFS 统计连通分量个数，并输出每个分量包含的顶点（升序）","额外输出每个分量的顶点数与最大分量的编号，处理孤立点（没有任何边）的情况","说明为什么对每个未访问顶点启动一次遍历就能恰好覆盖一个连通分量"],
  req_en:["Read n, m and m edges and store an undirected graph as an adjacency list vector<vector<int>> (vertices 1..n)","Count the connected components with DFS or BFS and print the vertices of each component in ascending order","Also print each component's size and the index of the largest one, handling isolated vertices with no edges","Explain why one traversal started at each unvisited vertex covers exactly one component"],
  starter:"#include <iostream>\n#include <vector>\n\nint main() {\n    int n, m;\n    std::cin >> n >> m;\n    std::vector<std::vector<int>> g(n + 1);\n    for (int i = 0; i < m; i++) {\n        int u, v; std::cin >> u >> v;\n        g[u].push_back(v);\n        g[v].push_back(u);\n    }\n    std::vector<bool> visited(n + 1, false);\n    // 对每个未访问顶点启动一次 DFS/BFS，统计每个分量的顶点\n    return 0;\n}",
  hint:"用 visited 标记已到达的点；外层循环每遇到一个未访问顶点就说明发现了一个新分量，内层遍历会恰好吸干该分量所有可达顶点。孤立点自己就是一个只有 1 个顶点的分量。",
  hint_en:"Mark reached vertices in visited. Each time the outer loop meets an unvisited vertex a new component begins; the inner traversal drains exactly that component. An isolated vertex is a component of size one.",
  xp:40},
 {t:"最短路径 BFS",t_en:"Shortest Path with BFS",
  req:["在无权网格中求从起点到终点的最短步数","用队列实现 BFS，并用 dist 数组记录步数","无法到达时返回 -1"],
  req_en:["Find the fewest steps from start to goal on an unweighted grid","Use a queue for BFS and a dist array for step counts","Return -1 when unreachable"],
  starter:`std::queue<std::pair<int,int>> q;\nq.push({sr, sc}); dist[sr][sc] = 0;\n// 逐层扩展，第一次到达终点即最短`,
  hint:"BFS 按层扩散，第一次到达终点的层数就是最短距离，这是 Dijkstra 在等权图上的特例。",
  hint_en:"BFS expands layer by layer, so the first arrival is the shortest — it's Dijkstra's special case on unweighted graphs.",
  xp:40}
],

"dsa":[
 {t:"快速排序分区实现",t_en:"Quicksort Partition",
  req:["手写 Lomuto 分区与递归快排","用随机数组验证排序正确性","说明最坏情况是什么、如何随机化避免"],
  req_en:["Implement Lomuto partitioning and recursive quicksort","Verify correctness on a random array","Describe the worst case and how randomization avoids it"],
  starter:`int partition(int a[], int l, int r);\nvoid quickSort(int a[], int l, int r) {\n    if (l >= r) return;\n    int p = partition(a, l, r);\n    quickSort(a, l, p - 1); quickSort(a, p + 1, r);\n}`,
  hint:"有序数组取末位做基准会退化成 O(n²)；随机选基准或三数取中可以规避。",
  hint_en:"Picking the last element as pivot on sorted input degrades to O(n²); random pivot or median-of-three avoids it.",
  xp:40},
 {t:"二分查找边界问题",t_en:"Binary Search Boundaries",
  req:["实现「找第一个 >= target 的位置」（lower_bound 语义）","实现「找最后一个 <= target 的位置」","用含重复元素的数组各测三次并说明 mid 计算为何写成 l+(r-l)/2"],
  req_en:["Implement 'first position with value >= target' (lower_bound semantics)","Implement 'last position with value <= target'","Test each three times on arrays with duplicates; explain why mid is l+(r-l)/2"],
  starter:`int lowerBound(const std::vector<int>& a, int target) {\n    int l = 0, r = (int)a.size();\n    while (l < r) { int m = l + (r - l) / 2; if (a[m] < target) l = m + 1; else r = m; }\n    return l;\n}`,
  hint:"写成 (l+r)/2 可能整数溢出；区分「找下界」和「找上界」的关键在等号放哪边。",
  hint_en:"(l+r)/2 can overflow; the difference between lower and upper bound is which side the equality goes.",
  xp:35}
],

/* ---------- 工程化与职业篇 ---------- */
"eng":[
 {t:"用 GDB 定位一次崩溃",t_en:"Pin Down a Crash with GDB",
  req:["写一段故意越界的代码（例如访问 10 个元素数组的第 11 个），让它崩溃","用 g++ -g 编译并在 GDB 里运行，让程序停在被访问出错的位置","用 backtrace 打印调用栈，指出出错的那一行并解释原因"],
  req_en:["Write code that intentionally overruns a 10-element array and crashes","Compile with g++ -g and run it under GDB so it stops at the faulty access","Print the call stack with backtrace and name the offending line"],
  starter:`#include <iostream>\nint main(){\n    int a[10];\n    for (int i = 0; i <= 10; ++i) a[i] = i;   // i==10 时越界\n    std::cout << a[10] << std::endl;\n}`,
  hint:"g++ -g -fsanitize=address 也能直接报出行号；GDB 里先 run，崩溃后敲 bt 看调用栈。",
  hint_en:"g++ -g -fsanitize=address also pinpoints the line; in GDB run first, then type bt for the stack.",
  xp:40},
 {t:"Makefile 与编译脚本",t_en:"Makefile & Build Script",mode:"text",
  req:["为一个 main.cpp + calc.cpp 的项目写一份 Makefile（含 clean 目标）","说明为什么改了头文件后需要重新编译依赖它的 .cpp","给出开启 -Wall -Wextra 后你会优先修哪类警告"],
  req_en:["Write a Makefile (with a clean target) for a main.cpp + calc.cpp project","Explain why touching a header forces recompiling the .cpp files that include it","Say which warning class you'd fix first after enabling -Wall -Wextra"],
  starter:`app: main.o calc.o\n\tg++ main.o calc.o -o app\n\n%.o: %.cpp\n\tg++ -Wall -Wextra -c $< -o $@`,
  hint:"Makefile 的配方行必须以 Tab 开头，用空格会报 missing separator。",
  hint_en:"Makefile recipe lines must start with a Tab; spaces give a 'missing separator' error.",
  xp:35}
],

"career":[
 {t:"自我介绍与项目陈述",t_en:"Self-intro & Project Pitch",mode:"text",
  req:["写一段 90 秒的自我介绍（技术背景 / 代表性项目 / 求职方向）","用 STAR 法描述你在本项目里解决过的一个真实技术难题","准备 3 个你想反问面试官的问题"],
  req_en:["Write a 90-second self-introduction (background / flagship project / target role)","Describe one real technical problem you solved in this project using STAR","Prepare three questions to ask the interviewer"],
  starter:`背景：\n项目（S/T/A/R）：\n反问：`,
  hint:"STAR = 情境 Situation / 任务 Task / 行动 Action / 结果 Result，结果尽量带数字。",
  hint_en:"STAR = Situation / Task / Action / Result — quantify the result whenever you can.",
  xp:30},
 {t:"简历技术点自查表",t_en:"Resume Tech-claim Checklist",mode:"text",
  req:["列出你简历上出现的 5 个技术关键词，每个写一句「能被追问到什么深度」","找出其中你只能答到一层的关键词，写出补强计划","把其中一个关键词讲给不懂技术的朋友听，检验表达是否清晰"],
  req_en:["List five technical keywords from your resume and note how deep each can be probed","Find the ones you can only answer one level on and write a plan to strengthen them","Explain one keyword to a non-technical friend to test your clarity"],
  starter:`关键词1：能被追问到 ——\n关键词2：能被追问到 ——\n...`,
  hint:"简历上每一个词都是面试官的靶子：写上去就要能答三层「为什么」。",
  hint_en:"Every word on your resume is a target — if it's on there, be able to answer three layers of 'why'.",
  xp:30}
],

/* ---------- Python 数据科学篇 ---------- */
"sp1":[
 {t:"NumPy 数组变形与广播",t_en:"NumPy Reshape & Broadcasting",
  req:["创建 3×4 的随机数组，分别做 reshape、转置、切片取子块","用广播给每一行减去该行均值","对比 np.mean(axis=0) 与 axis=1 的含义差异"],
  req_en:["Create a 3x4 random array; reshape, transpose and slice a sub-block","Subtract each row's mean using broadcasting","Contrast np.mean(axis=0) with axis=1"],
  starter:`import numpy as np\n\na = np.random.rand(3, 4)\ncentered = a - a.mean(axis=1, keepdims=True)`,
  hint:"keepdims=True 保留维度，广播才能按行对齐 —— 这是最常见的坑。",
  hint_en:"keepdims=True preserves the dimension so broadcasting aligns per row — the most common gotcha.",
  xp:30},
 {t:"图像即矩阵",t_en:"An Image Is Just a Matrix",
  req:["用 NumPy 生成一个 64×64 的灰度渐变图并打印其 shape 与 dtype","对矩阵做一次水平翻转与一次 90 度旋转","统计像素均值与标准差，说明它们和「亮度/对比度」的关系"],
  req_en:["Generate a 64x64 grayscale gradient with NumPy and print its shape and dtype","Flip it horizontally and rotate it 90 degrees","Report the pixel mean and std, and relate them to brightness and contrast"],
  starter:`import numpy as np\n\nimg = np.linspace(0, 255, 64*64).reshape(64, 64)\n# 翻转 / 旋转 / 统计`,
  hint:"np.rot90 与切片 [::-1] 是最省事的两种几何变换。",
  hint_en:"np.rot90 and slicing [::-1] are the cheapest geometric transforms.",
  xp:30}
],

"sp2":[
 {t:"Pandas 成绩表分析",t_en:"Grade Table Analysis with Pandas",
  req:["读入 CSV 成 DataFrame，查看前 5 行与基础统计信息","按班级分组求各科平均分，并找出总分最高的 3 人","新增一列「等级」按总分分档"],
  req_en:["Read a CSV into a DataFrame; inspect the first 5 rows and basic stats","Group by class for per-subject averages and find the top 3 by total","Add a 'grade' column bucketing the totals"],
  starter:`import pandas as pd\n\ndf = pd.read_csv("grades.csv")\nprint(df.head(), df.describe())`,
  hint:"分档用 pd.cut，分组聚合用 groupby().agg()，两者是 Pandas 最常用的组合拳。",
  hint_en:"pd.cut for bucketing and groupby().agg() for aggregation — the most common Pandas one-two punch.",
  xp:35},
 {t:"缺失值与可视化",t_en:"Missing Values & Visualization",
  req:["给数据人为制造缺失值，用 isna().sum() 定位","分别用删除、均值填充、前向填充三种方式处理并比较","画出成绩分布直方图与科目间散点图"],
  req_en:["Introduce missing values on purpose and locate them with isna().sum()","Handle them three ways — drop, mean-fill, forward-fill — and compare","Plot a histogram of scores and a scatter plot between subjects"],
  starter:`df.loc[3, "math"] = None\nprint(df.isna().sum())\ndf_filled = df.fillna(df.mean(numeric_only=True))`,
  hint:"填充方式会改变分布：均值填充压低方差，删行则损失样本量，按场景权衡。",
  hint_en:"Each strategy distorts something: mean-fill shrinks variance, dropping loses samples — weigh it per case.",
  xp:35}
],

"sp3":[
 {t:"Jupyter 探索性分析项目",t_en:"Jupyter EDA Project",mode:"text",
  req:["写一份 EDA 笔记大纲：数据概览 → 清洗 → 单变量分析 → 关系分析 → 结论","每个环节写出你会用的具体函数与想回答的问题","说明为什么 EDA 要在建模之前做"],
  req_en:["Outline an EDA notebook: overview → cleaning → univariate → relationships → conclusions","For each stage list the concrete functions and the question you're answering","Explain why EDA comes before modeling"],
  starter:`# 1. 概览：shape / info / describe\n# 2. 清洗：缺失、异常、重复\n# 3. 单变量：分布与离群点\n# 4. 关系：相关矩阵、分组对比`,
  hint:"EDA 的目标不是画图，而是形成「下一步该建什么模型」的假设。",
  hint_en:"The point of EDA isn't charts — it's forming a hypothesis about what model to build next.",
  xp:30},
 {t:"可复现实验记录",t_en:"Reproducible Experiment Log",mode:"text",
  req:["为一次数据分析实验写实验记录：数据来源、环境版本、随机种子、结论","说明为什么要固定随机种子与记录库版本","给出一条让 Notebook 可一键复现的实践建议"],
  req_en:["Write an experiment log: data source, environment versions, random seed, conclusions","Explain why you pin the seed and record library versions","Give one practice that makes a notebook reproducible in one command"],
  starter:`seed = 42\nnp.random.seed(seed)\n# 记录：numpy 版本 / pandas 版本 / 数据快照时间`,
  hint:"不可复现的结果等于没有结果 —— 种子、版本、数据快照三者缺一不可。",
  hint_en:"A result you can't reproduce is no result — seed, versions and a data snapshot are all mandatory.",
  xp:25}
],

/* ---------- 机器学习篇 ---------- */
"ml1":[
 {t:"手写一元线性回归",t_en:"Simple Linear Regression by Hand",
  req:["用最小二乘闭式解求 w、b（不调库）","用 NumPy 生成带噪声的数据验证拟合效果","手算 MSE 并说明它与损失函数的关系"],
  req_en:["Solve for w and b with the closed-form least-squares solution (no library)","Generate noisy data with NumPy and check the fit","Compute MSE by hand and relate it to the loss function"],
  starter:`import numpy as np\n\nx = np.random.rand(100)\ny = 3*x + 2 + np.random.randn(100)*0.2\n# w = Σ(x-x̄)(y-ȳ) / Σ(x-x̄)²`,
  hint:"先中心化 x 与 y，w 就是协方差除以方差，b 用均值反推。",
  hint_en:"Center x and y first; w is covariance over variance, and b follows from the means.",
  xp:35},
 {t:"过拟合观察实验",t_en:"Overfitting Observation Lab",
  req:["用多项式回归分别拟合 degree=1 / 5 / 15 并画出三条曲线","记录各自的训练集误差与测试集误差","说明哪一条曲线过拟合，以及两种缓解手段"],
  req_en:["Fit polynomial regression with degree = 1 / 5 / 15 and plot all three curves","Log training and test error for each","Say which one overfits and name two remedies"],
  starter:`from sklearn.preprocessing import PolynomialFeatures\nfrom sklearn.linear_model import LinearRegression\nfrom sklearn.pipeline import make_pipeline\n\nfor d in [1, 5, 15]:\n    model = make_pipeline(PolynomialFeatures(d), LinearRegression())`,
  hint:"训练误差持续下降但测试误差反弹，就是过拟合的典型信号；加正则或加数据是首选。",
  hint_en:"Training error keeps dropping while test error bounces back — the classic overfit signal. Regularize or add data.",
  xp:35}
],

"ml2":[
 {t:"手写 sigmoid 与逻辑回归",t_en:"Sigmoid & Logistic Regression by Hand",
  req:["实现 sigmoid 函数并画出曲线，说明它把什么映射到什么区间","用梯度下降训练一个二分类逻辑回归（不调库）","说明为什么用对数损失而不是均方误差"],
  req_en:["Implement sigmoid, plot it, and say what range it maps to","Train a binary logistic regression with gradient descent (no library)","Explain why log loss beats mean squared error here"],
  starter:`def sigmoid(z):\n    return 1 / (1 + np.exp(-z))\n\n# 梯度：grad_w = X.T @ (p - y) / n`,
  hint:"sigmoid 把实数压到 (0,1)；配对数损失时梯度恰好是 (p-y)，形式非常干净。",
  hint_en:"Sigmoid squashes reals into (0,1); paired with log loss the gradient collapses to (p-y) — very clean.",
  xp:40},
 {t:"多分类与 softmax",t_en:"Multiclass & Softmax",
  req:["实现 softmax 并验证每一行的和为 1","用 sklearn 训练一个三分类模型并输出预测概率","说明 softmax 与 sigmoid 的使用边界"],
  req_en:["Implement softmax and verify each row sums to 1","Train a three-class model with sklearn and print predicted probabilities","Explain where softmax ends and sigmoid begins"],
  starter:`def softmax(z):\n    e = np.exp(z - z.max(axis=1, keepdims=True))\n    return e / e.sum(axis=1, keepdims=True)`,
  hint:"减最大值是数值稳定技巧，避免 exp 溢出；多分类互斥用 softmax，多标签用 sigmoid。",
  hint_en:"Subtracting the max is a numerical-stability trick against exp overflow; softmax for mutually exclusive classes, sigmoid for multi-label.",
  xp:35}
],

"ml3":[
 {t:"交叉验证与评估指标",t_en:"Cross-validation & Metrics",
  req:["用 5 折交叉验证评估一个分类器，输出每折分数与均值","计算精确率、召回率、F1 并解释它们各自的侧重","在类别不平衡数据上说明为什么准确率会骗人"],
  req_en:["Evaluate a classifier with 5-fold CV; print each fold and the mean","Compute precision, recall and F1, and say what each emphasizes","Explain why accuracy misleads on imbalanced data"],
  starter:`from sklearn.model_selection import cross_val_score\nfrom sklearn.metrics import classification_report\n\nscores = cross_val_score(model, X, y, cv=5)`,
  hint:"正样本只占 1% 时全预测负类也有 99% 准确率 —— 所以要看召回率与 F1。",
  hint_en:"With 1% positives, always predicting negative scores 99% accuracy — that's why recall and F1 matter.",
  xp:35},
 {t:"特征工程实战",t_en:"Feature Engineering in Practice",
  req:["对一份含类别与数值特征的数据做独热编码与标准化","构造 2 个你认为有用的派生特征并说明业务含义","对比加特征前后的模型分数变化"],
  req_en:["One-hot encode and standardize a dataset with categorical and numeric features","Build two derived features you think will help and explain their business meaning","Compare the model score before and after"],
  starter:`from sklearn.compose import ColumnTransformer\nfrom sklearn.preprocessing import OneHotEncoder, StandardScaler`,
  hint:"先划分训练/测试集再做标准化，否则测试集信息会泄漏进训练过程。",
  hint_en:"Split before you standardize, otherwise test-set statistics leak into training.",
  xp:35}
],

"ml4":[
 {t:"决策树与可解释性",t_en:"Decision Trees & Interpretability",
  req:["训练一棵决策树并打印前几层分裂条件","画出特征重要性条形图并解释含义","说明单棵树为什么容易过拟合"],
  req_en:["Train a decision tree and print the first few split conditions","Plot feature importances and explain what they mean","Explain why a single tree overfits easily"],
  starter:`from sklearn.tree import DecisionTreeClassifier, export_text\n\nprint(export_text(tree, feature_names=list(X.columns)))`,
  hint:"树可以一路长到每个叶子只有一个样本 —— 必须用 max_depth 或 min_samples_leaf 约束。",
  hint_en:"A tree can grow until each leaf holds one sample — constrain it with max_depth or min_samples_leaf.",
  xp:35},
 {t:"集成学习对比",t_en:"Ensemble Methods Compared",
  req:["在同一数据集上训练 Bagging、RandomForest、GradientBoosting 并对比分数","说明 Bagging 降方差、Boosting 降偏差分别是什么意思","记录训练耗时，讨论精度与成本的权衡"],
  req_en:["Train Bagging, RandomForest and GradientBoosting on the same data and compare scores","Explain what 'bagging reduces variance' and 'boosting reduces bias' mean","Log training time and discuss the accuracy/cost trade-off"],
  starter:`from sklearn.ensemble import BaggingClassifier, RandomForestClassifier, GradientBoostingClassifier`,
  hint:"Bagging 并行、抗过拟合；Boosting 串行、精度高但对噪声和调参更敏感。",
  hint_en:"Bagging is parallel and resists overfitting; boosting is sequential, accurate, but sensitive to noise and hyperparameters.",
  xp:40}
],

/* ---------- 深度学习篇 ---------- */
"dl1":[
 {t:"从零搭一个神经元",t_en:"Build One Neuron from Scratch",
  req:["用 NumPy 实现 y = activation(Wx + b) 的前向传播","手动算出梯度并做一次参数更新","把它扩展成 3 层 MLP 的前向传播"],
  req_en:["Implement forward pass y = activation(Wx + b) with NumPy","Compute gradients by hand and do one parameter update","Extend it to the forward pass of a 3-layer MLP"],
  starter:`import numpy as np\n\nW = np.random.randn(3, 1) * 0.01\nb = np.zeros((1, 1))\nz = X @ W + b`,
  hint:"权重初始化太小梯度消失、太大梯度爆炸，0.01 量级的高斯是安全起点。",
  hint_en:"Too-small init kills gradients, too-large blows them up; a 0.01-scale Gaussian is a safe start.",
  xp:40},
 {t:"激活函数对比实验",t_en:"Activation Function Comparison",
  req:["实现 sigmoid / tanh / ReLU / LeakyReLU 并画在同一张图上","计算它们在 x=0 附近的导数，说明饱和区问题","说明为什么 ReLU 成为默认选择，以及它的死神经元问题"],
  req_en:["Implement sigmoid / tanh / ReLU / LeakyReLU and plot them together","Compute their derivatives near x=0 and explain the saturation problem","Explain why ReLU became the default, and its dying-neuron issue"],
  starter:`def relu(x): return np.maximum(0, x)\ndef leaky(x, a=0.01): return np.where(x > 0, x, a*x)`,
  hint:"sigmoid 两端导数趋近 0 → 梯度消失；ReLU 正区间导数恒为 1，但负区间一旦为 0 就再也学不回来。",
  hint_en:"Sigmoid's tails flatten to 0 → vanishing gradients; ReLU's derivative is 1 on the positive side but a neuron stuck at 0 never recovers.",
  xp:35}
],

"dl2":[
 {t:"PyTorch 训练循环",t_en:"PyTorch Training Loop",
  req:["用 torch.nn 搭一个两层网络，写完整的训练循环（forward / loss / backward / step）","每 10 个 epoch 打印一次 loss，观察下降趋势","把 optimizer.zero_grad() 去掉，观察并解释现象"],
  req_en:["Build a two-layer net with torch.nn and write the full training loop (forward / loss / backward / step)","Print loss every 10 epochs and watch it drop","Remove optimizer.zero_grad() and explain what you observe"],
  starter:`for epoch in range(100):\n    pred = model(x)\n    loss = criterion(pred, y)\n    opt.zero_grad()\n    loss.backward()\n    opt.step()`,
  hint:"PyTorch 的梯度是累加的，不清零就会把上一轮的梯度叠加上去，训练直接跑飞。",
  hint_en:"PyTorch accumulates gradients; without zeroing, last step's gradients stack up and training blows up.",
  xp:45},
 {t:"Dataset 与 DataLoader 实践",t_en:"Dataset & DataLoader Practice",
  req:["自定义一个 Dataset 子类实现 __len__ 与 __getitem__","用 DataLoader 做批量加载与打乱，打印第一个 batch 的形状","说明 batch_size 与 shuffle 对训练的影响"],
  req_en:["Write a custom Dataset subclass with __len__ and __getitem__","Use DataLoader for batching and shuffling; print the first batch's shape","Explain how batch_size and shuffle affect training"],
  starter:`from torch.utils.data import Dataset, DataLoader\n\nclass MyData(Dataset):\n    def __len__(self): return len(self.x)\n    def __getitem__(self, i): return self.x[i], self.y[i]`,
  hint:"训练时 shuffle=True 打破样本顺序相关性；验证时关掉以保证结果可复现。",
  hint_en:"Shuffle during training to break order correlation; turn it off for validation to keep results reproducible.",
  xp:40}
],

"dl3":[
 {t:"手写卷积运算",t_en:"Convolution from Scratch",
  req:["用 NumPy 实现单通道 2D 卷积（含 stride 与 padding）","用同一个边缘检测核对一张图做卷积并观察效果","说明卷积相比全连接少了多少参数"],
  req_en:["Implement single-channel 2D convolution with NumPy (stride and padding included)","Apply an edge-detection kernel to an image and look at the result","Explain how many parameters convolution saves versus a fully connected layer"],
  starter:`import numpy as np\n\ndef conv2d(img, k, stride=1, pad=0):\n    # 补边 -> 滑窗 -> 逐点乘加\n    pass`,
  hint:"参数共享 + 局部连接是卷积的两大法宝：同一核扫全图，参数量与图像大小无关。",
  hint_en:"Weight sharing plus local connectivity: one kernel sweeps the whole image, so parameter count is independent of image size.",
  xp:45},
 {t:"CNN 图像分类小项目",t_en:"Small CNN Image Classifier",
  req:["用 PyTorch 搭一个 Conv-ReLU-Pool 堆叠的小 CNN","在小型数据集上训练 5 个 epoch 并输出准确率","加入数据增强（随机翻转）后对比效果"],
  req_en:["Stack Conv-ReLU-Pool into a small CNN with PyTorch","Train 5 epochs on a small dataset and report accuracy","Add random-flip augmentation and compare"],
  starter:`import torch.nn as nn\n\nmodel = nn.Sequential(\n    nn.Conv2d(3, 16, 3, padding=1), nn.ReLU(), nn.MaxPool2d(2),\n    nn.Flatten(), nn.Linear(16*16*16, 10)\n)`,
  hint:"池化让特征图变小、通道数变多，是 CNN 的标准节奏。",
  hint_en:"Shrink spatial size while growing channels — that's the standard CNN rhythm.",
  xp:45}
],

"dl4":[
 {t:"RNN 与梯度消失",t_en:"RNN & Vanishing Gradients",
  req:["用 NumPy 手写一个 RNN 单元并按时间步展开","观察长序列上梯度随步数的衰减趋势","说明 LSTM 的门控如何缓解这个问题"],
  req_en:["Write an RNN cell with NumPy and unroll it over time","Watch gradients decay over long sequences","Explain how LSTM gating alleviates the problem"],
  starter:`h = np.zeros((hidden,))\nfor t in range(T):\n    h = np.tanh(W_xh @ x[t] + W_hh @ h)`,
  hint:"每一步都乘一次 W_hh，步数一多梯度就像 0.9^100 一样趋零 —— 这就是消失。",
  hint_en:"Every step multiplies by W_hh, so after many steps the gradient behaves like 0.9^100 — that's vanishing.",
  xp:45},
 {t:"注意力机制手写实现",t_en:"Attention from Scratch",
  req:["用 NumPy 实现缩放点积注意力 softmax(QKᵀ/√d)V","验证注意力权重每行和为 1","说明为什么要除以 √d"],
  req_en:["Implement scaled dot-product attention softmax(QKᵀ/√d)V with NumPy","Verify each row of attention weights sums to 1","Explain why you divide by √d"],
  starter:`scores = Q @ K.T / np.sqrt(d_k)\nweights = softmax(scores)\nout = weights @ V`,
  hint:"不除以 √d 时点积方差随维度增长，softmax 会被推进饱和区，梯度趋零。",
  hint_en:"Without √d the dot-product variance grows with dimension, pushing softmax into saturation where gradients vanish.",
  xp:45}
],

"dl5":[
 {t:"自回归生成采样",t_en:"Autoregressive Sampling",
  req:["实现一个简化版自回归生成：每步采样一个 token 并拼回输入","对比贪婪解码与带 temperature 采样的输出差异","说明 temperature 趋近 0 与趋近无穷时的极端行为"],
  req_en:["Implement simplified autoregressive generation: sample one token per step and append it","Compare greedy decoding with temperature sampling","Describe the extremes as temperature approaches 0 and infinity"],
  starter:`for _ in range(max_len):\n    logits = model(context)\n    probs = softmax(logits / temperature)\n    nxt = np.random.choice(len(probs), p=probs)`,
  hint:"temperature 越低越接近贪婪（确定但呆板），越高越均匀（发散但随机）。",
  hint_en:"Lower temperature approaches greedy (deterministic but dull); higher approaches uniform (diverse but incoherent).",
  xp:45},
 {t:"Transformer 模块拼装",t_en:"Assemble a Transformer Block",
  req:["用 PyTorch 的 nn.MultiheadAttention 拼一个 Transformer 编码层（含残差与 LayerNorm）","验证输入与输出形状一致","说明残差连接在这里解决了什么问题"],
  req_en:["Assemble a Transformer encoder layer with nn.MultiheadAttention (residual + LayerNorm)","Verify input and output shapes match","Explain what the residual connection solves here"],
  starter:`x = x + attn(x, x, x)[0]\nx = layer_norm(x)\nx = x + ffn(x)`,
  hint:"残差给梯度一条直通的高速公路，几十层堆起来也能训得动。",
  hint_en:"Residuals give gradients a direct highway, so dozens of layers still train.",
  xp:50}
],

/* ---------- 强化学习与 AGI 前沿篇 ---------- */
"agi1":[
 {t:"多臂老虎机 ε-greedy",t_en:"Multi-armed Bandit with ε-greedy",
  req:["实现 k=5 的多臂老虎机环境（每台机器有不同真实回报率）","实现 ε-greedy 策略，ε 取 0.0 / 0.1 / 0.3 各跑 1000 步","画图对比累计收益，说明探索与利用的权衡"],
  req_en:["Implement a k=5 bandit environment (each arm with a different true payoff)","Implement ε-greedy with ε = 0.0 / 0.1 / 0.3 over 1000 steps","Plot cumulative reward and discuss the exploration/exploitation trade-off"],
  starter:`import numpy as np\n\nq = np.zeros(5)      # 估计价值\nn = np.zeros(5)      # 每臂被拉次数\nif np.random.rand() < eps:\n    a = np.random.randint(5)\nelse:\n    a = np.argmax(q)`,
  hint:"ε=0 可能永远锁死在次优臂上；ε 太大又浪费大量步数在探索上。",
  hint_en:"ε=0 can lock onto a suboptimal arm forever; too large an ε wastes most steps exploring.",
  xp:40},
 {t:"Q-learning 走迷宫",t_en:"Q-learning in a Grid World",
  req:["实现 4×4 网格世界：到达终点 +1，掉陷阱 -1，其余 0","用 Q-learning 更新 Q 表，训练 500 回合","打印最终 Q 表并说明它如何体现「往哪走更好」"],
  req_en:["Build a 4x4 grid world: +1 at the goal, -1 at traps, 0 elsewhere","Update a Q table with Q-learning for 500 episodes","Print the final Q table and explain how it encodes 'which way is better'"],
  starter:`Q[s, a] += lr * (r + gamma * np.max(Q[s2]) - Q[s, a])`,
  hint:"Q 值学到的是「长期折扣回报」，所以它会自动权衡绕路与抄近道。",
  hint_en:"Q learns long-term discounted return, so it automatically weighs detours against shortcuts.",
  xp:45}
],

"agi2":[
 {t:"手写分词与词表",t_en:"Tokenizer & Vocabulary by Hand",
  req:["实现一个简单的 BPE 风格词表统计：统计字符对频率并合并最高频的一对","给出词表大小与压缩率的估算方法","说明为什么子词分词能兼顾词表大小与未登录词"],
  req_en:["Implement simple BPE-style stats: count character pairs and merge the most frequent one","Show how to estimate vocab size and compression ratio","Explain why subword tokenization balances vocab size against unknown words"],
  starter:`from collections import Counter\n\npairs = Counter()\nfor w in words:\n    for a, b in zip(w, w[1:]):\n        pairs[(a, b)] += 1`,
  hint:"按字符切不会有未登录词但序列太长；按词切序列短但词表爆炸 —— 子词是折中。",
  hint_en:"Characters mean no OOV but long sequences; words mean short sequences but huge vocab — subwords sit in between.",
  xp:40},
 {t:"Prompt 工程对照实验",t_en:"Prompt Engineering A/B Test",mode:"text",
  req:["针对同一个任务写 3 版提示词：零样本 / 少样本 / 带角色与格式约束","记录三版输出的质量差异并分析原因","总结一条你验证有效的提示词编写原则"],
  req_en:["Write three prompt versions for the same task: zero-shot / few-shot / with role and format constraints","Log the quality differences and analyze why","Summarize one prompt-writing principle you verified works"],
  starter:`# v1 零样本：直接提问\n# v2 少样本：给 2~3 个示例\n# v3 角色 + 输出格式 + 约束条件`,
  hint:"给出示例与明确输出格式通常比「请详细一点」这类空泛要求有效得多。",
  hint_en:"Examples plus an explicit output format usually beat vague instructions like 'please be detailed'.",
  xp:30}
],

"agi3":[
 {t:"最小 Agent 循环",t_en:"Minimal Agent Loop",
  req:["实现一个 ReAct 风格循环：思考 → 调用工具 → 观察结果 → 再思考","给它 2 个工具（计算器 / 搜索占位函数），完成一个需要两步的问题","打印每一轮的思考与观察，说明循环终止条件怎么定"],
  req_en:["Implement a ReAct-style loop: think → call tool → observe → think again","Give it two tools (a calculator and a search stub) and solve a two-step question","Print each round's thought and observation; define the stop condition"],
  starter:`for step in range(max_steps):\n    thought = llm(prompt + history)\n    if "Final:" in thought: break\n    obs = call_tool(parse(thought))`,
  hint:"必须设最大步数上限，否则模型可能在工具之间无限打转。",
  hint_en:"Always cap the step count, otherwise the model can loop between tools forever.",
  xp:45},
 {t:"多模态输入对齐",t_en:"Multimodal Input Alignment",mode:"text",
  req:["描述图像编码器与文本编码器如何映射到同一个向量空间","说明对比学习在这里的作用（以 CLIP 为例）","给出一个你能想到的多模态落地场景并指出技术难点"],
  req_en:["Describe how an image encoder and a text encoder map into a shared vector space","Explain the role of contrastive learning here (CLIP as the example)","Propose a multimodal application and name its technical difficulty"],
  starter:`# image -> I_vec\n# text   -> T_vec\n# 目标：配对的 (I, T) 相似度高，不配对的低`,
  hint:"关键是让「同一语义的不同模态」在向量空间里靠近，靠的是大批量的正负样本对比。",
  hint_en:"The trick is pulling different modalities of the same meaning together, via contrast over large batches of positives and negatives.",
  xp:35}
],

"agi4":[
 {t:"AI 项目落地方案设计",t_en:"AI Project Delivery Plan",mode:"text",
  req:["为一个真实场景（如客服问答）写一份落地方案：目标、数据、模型、评估、风险","给出离线指标与线上指标各两个，并说明它们的差距","列出三条你认为最关键的风险与应对"],
  req_en:["Write a delivery plan for a real scenario (e.g. support QA): goal, data, model, evaluation, risks","Give two offline metrics and two online metrics, and explain the gap between them","List the three most critical risks and your mitigations"],
  starter:`目标：\n数据与模型：\n离线指标 / 线上指标：\n风险与应对：`,
  hint:"离线涨点不等于线上收益 —— 一定要设计能归因到业务指标的评估方式。",
  hint_en:"Offline gains don't equal online wins — design evaluation you can attribute to a business metric.",
  xp:35},
 {t:"推理成本与延迟优化",t_en:"Inference Cost & Latency",mode:"text",
  req:["估算一次大模型调用的 token 成本与首字延迟构成","列出 4 种降本增效手段（缓存 / 批处理 / 量化 / 蒸馏等）并给出适用场景","说明在什么情况下你会选择小模型而不是大模型"],
  req_en:["Estimate the token cost of one LLM call and what makes up time-to-first-token","List four cost/latency levers (caching, batching, quantization, distillation) with when each applies","Say when you'd pick a small model over a large one"],
  starter:`成本 = (输入 token × 单价 + 输出 token × 单价) × 调用量\n延迟 ≈ 排队 + 首字 + 生成`,
  hint:"多数线上场景瓶颈是输出 token 的生成时间与排队，不是输入长度。",
  hint_en:"In most production setups the bottleneck is output-token generation and queueing, not input length.",
  xp:35}
],

/* ---------- Python 路线篇 ---------- */
"py1":[
 {t:"Python 名片生成器",t_en:"Python Business Card Generator",
  req:["用三个变量存昵称、职业方向、当前目标，用 f-string 拼成一张名片","名片包含分隔线，至少 5 行","把名片同时打印两遍（第二次换一个分隔符样式）"],
  req_en:["Store nickname, career direction and current goal in three variables; compose a card with an f-string","The card has a separator line and at least 5 lines","Print the card twice, changing the separator style the second time"],
  starter:`name = "你的昵称"\ngoal = "本周目标"\nprint("=" * 20)\nprint(f"昵称：{name}")`,
  hint:"f-string 前加 f 才能用 {} 插值；'=' * 20 可以快速生成重复字符。",
  hint_en:"Prefix the string with f to interpolate {}; '=' * 20 generates repeated characters quickly.",
  xp:20},
 {t:"单位换算小程序",t_en:"Unit Converter",
  req:["读入公里数，输出对应的英里数（1 km = 0.621371 mi）","结果保留 2 位小数","输入为负数时给出提示而不是直接计算"],
  req_en:["Read a distance in km and print the equivalent in miles (1 km = 0.621371 mi)","Round to 2 decimals","If the input is negative, print a hint instead of computing"],
  starter:`km = float(input("请输入公里数："))\n# 判断正负 -> 换算 -> 格式化输出`,
  hint:"f\"{miles:.2f}\" 直接控制小数位数，比 round() 更适合输出场景。",
  hint_en:"f\"{miles:.2f}\" controls decimals directly — better than round() for display.",
  xp:25}
],

"py2":[
 {t:"列表统计与筛选",t_en:"List Statistics & Filtering",
  req:["给定一组成绩列表，用列表推导式筛出及格项","用一行代码求平均分、最高分、最低分","统计各分数段人数并存入字典"],
  req_en:["Given a list of scores, filter the passing ones with a list comprehension","Compute average, max and min each in one line","Count how many fall in each score band and store in a dict"],
  starter:`scores = [55, 78, 91, 62, 88, 45]\npassed = [s for s in scores if s >= 60]`,
  hint:"列表推导式 [expr for x in xs if cond] 是 Python 最常用的惯用法之一。",
  hint_en:"[expr for x in xs if cond] is one of Python's most idiomatic constructs.",
  xp:25},
 {t:"字典词频统计",t_en:"Word Frequency with dict",
  req:["读入一段英文文本，用字典统计词频","输出出现次数最多的 3 个词","用 collections.Counter 重写一遍并对比代码量"],
  req_en:["Read a piece of English text and count word frequencies with a dict","Print the 3 most frequent words","Rewrite it with collections.Counter and compare the amount of code"],
  starter:`from collections import Counter\n\nwords = text.lower().split()\ntop3 = Counter(words).most_common(3)`,
  hint:"dict.get(k, 0) + 1 是手写计数的经典写法，Counter.most_common 则一行搞定。",
  hint_en:"dict.get(k, 0) + 1 is the classic manual counter; Counter.most_common does it in one line.",
  xp:25}
],

"py3":[
 {t:"文件读写与异常处理",t_en:"File I/O with Exception Handling",
  req:["用 with open 写入 5 行日记到 diary.txt","再读回来逐行打印并统计总行数","捕获 FileNotFoundError 并给出友好提示"],
  req_en:["Write 5 diary lines to diary.txt using with open","Read them back, print line by line and count the total","Catch FileNotFoundError with a friendly message"],
  starter:`try:\n    with open("diary.txt", encoding="utf-8") as f:\n        lines = f.readlines()\nexcept FileNotFoundError:\n    print("文件不存在")`,
  hint:"with 语句会自动关闭文件，即使中途抛异常也安全。",
  hint_en:"The with statement closes the file automatically, even if an exception is raised midway.",
  xp:30},
 {t:"模块化拆分练习",t_en:"Split into Modules",
  req:["把计算相关函数放到 mymath.py，主程序 main.py 用 import 引入","为 mymath.py 写一段 if __name__ == '__main__' 的自测代码","说明 __name__ 惯用法的作用"],
  req_en:["Put calculation helpers in mymath.py and import them from main.py","Add an if __name__ == '__main__' self-test block to mymath.py","Explain what the __name__ idiom is for"],
  starter:`# mymath.py\ndef add(a, b): return a + b\n\nif __name__ == "__main__":\n    print(add(1, 2))`,
  hint:"直接运行时 __name__ 是 __main__，被 import 时是模块名 —— 所以自测代码不会在引入时执行。",
  hint_en:"__name__ is __main__ when run directly and the module name when imported — so self-tests don't fire on import.",
  xp:30}
],

"py4":[
 {t:"学生类与成绩管理",t_en:"Student Class & Grade Manager",
  req:["定义 class Student，含 name、scores 列表与 average() 方法","实现 add_score 与 __str__ 让打印更友好","创建 3 名学生并按平均分排序输出"],
  req_en:["Define class Student with name, a scores list and an average() method","Implement add_score and __str__ for friendly printing","Create 3 students and print them sorted by average"],
  starter:`class Student:\n    def __init__(self, name):\n        self.name = name\n        self.scores = []\n\n    def average(self):\n        return sum(self.scores) / len(self.scores)`,
  hint:"排序用 sorted(students, key=lambda s: s.average(), reverse=True)。",
  hint_en:"Sort with sorted(students, key=lambda s: s.average(), reverse=True).",
  xp:35},
 {t:"生成器与推导式",t_en:"Generators & Comprehensions",
  req:["写一个斐波那契生成器函数（用 yield）","用生成器表达式求 1~1000 中能被 3 整除的数之和","说明生成器相比列表在内存上的优势"],
  req_en:["Write a Fibonacci generator function using yield","Use a generator expression to sum numbers 1~1000 divisible by 3","Explain the memory advantage of generators over lists"],
  starter:`def fib(n):\n    a, b = 0, 1\n    for _ in range(n):\n        yield a\n        a, b = b, a + b`,
  hint:"生成器惰性求值，不会一次性把所有元素放进内存 —— 处理大文件尤其重要。",
  hint_en:"Generators are lazy and never materialize everything at once — crucial for large files.",
  xp:30}
]
};
